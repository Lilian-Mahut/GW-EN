// décompte du nombre d'heures de l'event à partir de sa date de début et si inf à 0 > fin
// https://www.npmjs.com/package/date-and-time
// else nothing

// https://moment.github.io/luxon/#/?id=luxon

// avec le package date and time ->
// date début -> affiche & si on soustrait date de fin > 0 ->> fin

const event = require("./config/event.json")
